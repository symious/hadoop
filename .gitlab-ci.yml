stages:
  - compile
  - pretest
  - test

pretest:
  stage: pretest
  tags:
    - hadoop2
  script:
    -  ./submodule-ci.sh
  artifacts:
    paths:
      - submodule-ci.yml

submodule-test:
  stage: test
  trigger:
    strategy: depend
    include:
      - artifact: submodule-ci.yml
        job: pretest


compile:
  stage: compile
  tags:
    - hadoop2
  script:
    - mvn clean install package -DskipTests
