variables:
  HADOOP_PROTOC_PATH: /usr/bin/protoc

stages:
  - compile
  - pretest
  - test

pretest:
  stage: pretest
  tags:
    - yarn
    - k8s
  script:
    -  ./submodule-ci.sh
  artifacts:
    paths:
      - submodule-ci.yml

submodule-test:
  stage: test
  trigger:
    strategy: depend
    include:
      - artifact: submodule-ci.yml
        job: pretest


compile:
  stage: compile
  tags:
    - yarn
    - k8s
  script:
    - mvn install -DskipTests
